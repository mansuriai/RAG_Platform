{
    "build": {
      "builder": "nixpacks",
      "config": {
        "startCmd": "streamlit run automated_platform/app/platform_ui.py --server.port $PORT --server.address 0.0.0.0"
      }
    }
  }